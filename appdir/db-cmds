--- Terminal commands ---
python
from appdir import db
db.create_all()  # creates database

# now we will add data using cmd line

from models import User, SensorTypes, Sensor
user_1 = User(username='Corey', email='C@demo', password='password')
db.session.add(user_1)
user_2 = User(username='JohnDoe', email='JD@demo', password='password')
db.session.add(user_2)
post_1 = Post(title='Blog 1', content='First Post', user_id=user.id)
db.session.add(post_1)
post_2 = Post(title='Blog 2', content='Second Post', user_id=user.id)
db.session.add(post_2)

db.session.commit()  # commit the changes

# SQL queries using SQLAlchemy

User.query.all()
User.query.first()
User.query.filter_by(username='Corey').all()

db.drop_all() # inverse of create_all(), destroys all

--- End ---

in account.html <!-- can also use {% set sen_str = sensor[loop.index-1] %} -->

in models.py # desc = db.Column(db.String(100), nullable=True)
